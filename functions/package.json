{
  "name": "rsms-payment-functions",
  "private": true,
  "version": "1.0.0",
  "description": "Verified payment and user-provisioning functions for RSMS",
  "main": "src/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "test": "node --test test/*.test.js",
    "check": "node --check src/gateway.js && node --check src/index.js"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0"
  }
}
